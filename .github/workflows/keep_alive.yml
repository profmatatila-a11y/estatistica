name: Keep Alive

on:
  schedule:
    - cron: '0 12 */2 * *' # Roda a cada 2 dias ao meio-dia UTC
  workflow_dispatch: # Permite rodar manualmente pelo GitHub para testar

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Site
        run: |
          # Substitua pelo URL real do seu site na Vercel
          curl -I https://estatistica-profmatatila-a11y.vercel.app
